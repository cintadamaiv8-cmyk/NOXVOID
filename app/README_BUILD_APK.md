# Build debug APK workflow

This workflow builds a debug APK for the Android app on push to branch `main` or manual dispatch. The produced APK artifact can be downloaded from the Actions run page.

NOTE: building on `main` ensures you can get APKs directly after merge.
